<!DOCTYPE html>
<html>
  <head>
    <title>Cookbook</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet" />
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css" rel="stylesheet" />
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <nav>
    <% if user_signed_in? %>
      <p><%= link_to 'Enviar uma receita', new_recipe_path %><p>
      <% if current_user.lists.any?%>
        <h1><%= link_to 'Minhas listas de receitas', lists_path %></h1>
      <% else %>
        <p><%= link_to 'Criar nova lista', new_list_path %><p>
      <% end %>
      <p><%= link_to 'Minhas receitas', my_recipes_path %><p>
      <% if current_user.admin? %>
        <p><%= link_to 'Receitas a serem avaliadas', evaluate_recipes_path %></p>
      <% end %>
      <p><%= link_to "Logout", destroy_user_session_path, method: :delete %></p>
    <% else %>
      <p><%= link_to "Fazer Login", new_user_session_path %></p>
      <p><%= link_to "Criar novo usuário", new_user_registration_path %></p>
    <% end %>
    </nav>

    <div class='container'>
      <%= yield %>
    </div>
    <div class="card-footer d-flex justify-content-center"">
      <p>Social Recipes | Copyright 2019</p>
      <div class="icons">
        <a href="#"><i class="fa fa-facebook"></i></a>
        <a href="#"><i class="fa fa-twitter"></i></a>
      </div>
    </div>
  </body>
</html>